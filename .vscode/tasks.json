{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "open connector",
            "type": "shell",
            "command": "node main.js MANAGER",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}",
            }
        },
        {
            "label": "kill cline connect",
            "type": "shell",
            "command": "pkill -f \"cline connect\" || true",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": false
            },
            "options": {
                "cwd": "${workspaceFolder}",
            }
        }
    ]
}