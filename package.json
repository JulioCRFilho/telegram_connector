{
  "name": "telegram-connector",
  "version": "2.0.0",
  "private": true,
  "description": "Wrapper that keeps cline telegram connectors alive through a key×model rotation grid (auto-resume, cooldown persistence, pre-flight probing).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node test.rotation.sim.js && node test.park-stale-grid.js && node test.probe.js && node test.pids-race.js && node test.task-progress.js && node test.timeout-rotation.js && node test.health-check.js && node test.model-priority.js && node test.interrupt.js && node test.watch-park.js && node test.park-refresh.js && node test.reset-command.js && node test.resume-memory.js && node test.health-feedback.js",
    "reset": "node reset-grid.js",
    "watch": "node watch-agents.js"
  }
}